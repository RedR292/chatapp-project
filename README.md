# chatapp-project
A chatapp for students built on python and using GCP as a remote database

# Requirements:
install websockets with pip
# On Linux:
`sudo apt install python3-websockets`
# On Windows:
`pip install websockets`
